replicaCount: 1

image:  
  repository: lehardocker/gitops-app
  pullPolicy: IfNotPresent
  tag: "1.0.0"

serviceAccount:
  create: false

service:
  type: ClusterIP
  port: 80
  targetPort: 3000  

ingress:
  enabled: false

autoscaling:
  enabled: false

